version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install uv
      - uv sync --frozen
run:
  command: uv run uvicorn app:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
